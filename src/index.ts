/**
 * Public entry point for the `agentflow` package.
 *
 * Pull components, primitives, and event types from here; adapters live at
 * `agentflow/adapters`; styles at `agentflow/styles.css`.
 *
 * @example
 *   import { ConversationView } from "agentflow";
 *   import { openAIToEvents } from "agentflow/adapters";
 *   import "agentflow/styles.css";
 */

export * from "./primitives";
export * from "./components";
export * from "./types/events";
