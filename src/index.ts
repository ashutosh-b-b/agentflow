/**
 * Public entry point for the `agentflow-ui` package.
 *
 * Pull components, primitives, and event types from here; adapters live at
 * `agentflow-ui/adapters`; styles at `agentflow-ui/styles.css`.
 *
 * @example
 *   import { ConversationView } from "agentflow-ui";
 *   import { openAIToEvents } from "agentflow-ui/adapters";
 *   import "agentflow-ui/styles.css";
 */

export * from "./primitives";
export * from "./components";
export * from "./types/events";
